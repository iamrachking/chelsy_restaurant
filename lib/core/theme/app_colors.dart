import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors (Marron foncé des maquettes)
  static const Color primary = Color(0xFF301911);
  static const Color primaryLight = Color(0xFF8D6E63);
  static const Color primaryDark = Color(0xFF3E2723);

  // Background beige clair
  static const Color backgroundBeige = Color(0xFFFCF6EB);

  // Secondary Colors
  static const Color secondary = Color(0xFFFFA726);
  static const Color secondaryLight = Color(0xFFFFD54F);
  static const Color secondaryDark = Color(0xFFFF8F00);

  // Accent Colors
  static const Color accent = Color(0xFFFFD54F);
  static const Color accentLight = Color(0xFFFFECB3);
  static const Color accentDark = Color(0xFFFFC107);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF43A047);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);
  static const Color info = Color(0xFF2196F3);

  // Border & Divider
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFE0E0E0);

  // Order Status Colors
  static const Color orderPending = Color(0xFFFFB300);
  static const Color orderConfirmed = Color(0xFF2196F3);
  static const Color orderPreparing = Color(0xFF9C27B0);
  static const Color orderReady = Color(0xFF00BCD4);
  static const Color orderDelivered = Color(0xFF43A047);
  static const Color orderCancelled = Color(0xFFE53935);
  static const Color orderStatusOutForDelivery = Color(0xFF00BCD4);

  // Additional Colors (pour éviter les couleurs hardcodées)
  static const Color orangeLight = Color(0xFFFFECB3);
  static const Color orangeMedium = Color(0xFFFFB74D);
  static const Color orangeDark = Color(0xFFE65100);
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color greyMedium = Color(0xFF9E9E9E);
  static const Color greyDark = Color(0xFF424242);
  static const Color amber = Color(0xFFFFC107);
  static const Color amberDark = Color(0xFFFF8F00);
  static const Color red = Color(0xFFE53935);
  static const Color redLight = Color(0xFFEF5350);

  // Standard Material Colors (pour compatibilité)
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;

  // Facebook & Google Colors
  static const Color facebookBlue = Color(0xFF1877F2);
  static const Color googleRed = Color(0xFFEA4335);
  static const Color googleBlue = Color(0xFF4285F4);
  static const Color googleYellow = Color(0xFFFBBC05);
  static const Color googleGreen = Color(0xFF34A853);
}
